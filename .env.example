# PostgreSQL connection string used by Prisma.
# Local dev example (matches the docker-compose / README instructions):
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/kbeauty"

# Optional: base URL of the site, used for canonical/OG tags.
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
